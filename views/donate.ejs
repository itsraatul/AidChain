<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <style>
        body { font-family: sans-serif; margin: 2em; }
        form { display: flex; flex-direction: column; width: 300px; gap: 10px; }
        input { padding: 8px; }
        button { padding: 10px; background-color: #4CAF50; color: white; border: none; cursor: pointer; }
    </style>
</head>
<body>
    <h1><%= pageTitle %></h1>
    <p>Enter your details below to make a pledge. You can change your pledge amount later!</p>
    <form action="/donate" method="POST">
        <input type="text" name="name" placeholder="Your Name" required>
        <input type="text" name="wallet_address" placeholder="Your Wallet Address" required>
        <input type="number" name="amount" placeholder="Pledge Amount (₹)" required>
        <button type="submit">Pledge Now</button>
    </form>
</body>
</html>